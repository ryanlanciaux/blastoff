import React from "react"
import { render } from "@testing-library/react"

import <%= props.name %> from './<%= props.name %>';

it("matches snapshot for <%= props.name %>", () => {
  const { container } = render(<h1>Hi</h1>);
})
