module.exports.typeValidator = function typeValidator(props) {
  if (props.type === undefined) {
    throw 'Type cannot be undefined'
  }
}

module.exports.nameValidator = function nameValidator(props) {
  if (props.type === undefined) {
    throw 'Type cannot be undefined'
  }
}
